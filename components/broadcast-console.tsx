'use client'

import { useState } from 'react'
import { Activity, Archive, ArrowUpRight, Bell, BookOpen, Check, ChevronDown, CircleHelp, Clock3, Code2, FileText, Filter, Gauge, Inbox, LayoutDashboard, Mail, Menu, MoreHorizontal, Plus, Search, Send, Settings2, ShieldCheck, Sparkles, Users, X } from 'lucide-react'
import { Button } from '@/components/ui/button'

type View = 'Overview' | 'Campaigns' | 'Subscribers' | 'Templates' | 'Settings'

const nav = [
  { label: 'Overview', icon: LayoutDashboard },
  { label: 'Campaigns', icon: Send },
  { label: 'Subscribers', icon: Users },
  { label: 'Templates', icon: FileText },
]
const subscribers = [
  ['Maya Chen', 'maya.chen@example.com', 'AWS Student', 'Subscribed', 'Aug 26, 2026'],
  ['Jon Bell', 'jon.bell@example.com', 'AWS Student', 'Subscribed', 'Aug 25, 2026'],
  ['Priya Shah', 'priya.shah@example.com', 'Newsletter', 'Subscribed', 'Aug 24, 2026'],
  ['Noah Wilson', 'noah.wilson@example.com', 'AWS Student', 'Pending', 'Aug 24, 2026'],
  ['Sofia Kim', 'sofia.kim@example.com', 'Newsletter', 'Subscribed', 'Aug 23, 2026'],
]

function Logo() { return <div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-[10px] bg-primary text-primary-foreground"><Activity className="size-5" /></div><div><p className="font-mono text-[11px] font-bold uppercase tracking-[0.2em] text-foreground">BASESTACK</p><p className="font-mono text-[10px] uppercase tracking-[0.12em] text-muted-foreground">BROADCAST</p></div></div> }
function StatusDot({ color = 'bg-primary' }: { color?: string }) { return <span className={`inline-block size-2 rounded-full ${color}`} /> }
function Badge({ children, tone = 'default' }: { children: React.ReactNode; tone?: 'default' | 'success' | 'warning' }) { return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 font-mono text-[10px] uppercase tracking-wide ${tone === 'success' ? 'bg-primary/10 text-primary' : tone === 'warning' ? 'bg-accent/20 text-accent-foreground' : 'bg-muted text-muted-foreground'}`}><StatusDot color={tone === 'success' ? 'bg-primary' : tone === 'warning' ? 'bg-accent-foreground' : 'bg-muted-foreground'} />{children}</span> }

function Sidebar({ view, setView, open, setOpen }: { view: View; setView: (v: View) => void; open: boolean; setOpen: (v: boolean) => void }) {
  return <aside className={`${open ? 'flex' : 'hidden'} fixed inset-y-0 left-0 z-20 w-64 flex-col border-r border-border bg-sidebar p-5 lg:static lg:flex`}>
    <div className="flex items-center justify-between"><Logo /><button className="lg:hidden" onClick={() => setOpen(false)} aria-label="Close navigation"><X className="size-5" /></button></div>
    <div className="mt-10 flex flex-col gap-1">{nav.map(item => { const Icon = item.icon; return <button key={item.label} onClick={() => { setView(item.label as View); setOpen(false) }} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm transition ${view === item.label ? 'bg-primary/10 font-medium text-primary' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon className="size-4" />{item.label}{item.label === 'Campaigns' && <span className="ml-auto rounded-full bg-primary/15 px-2 py-0.5 font-mono text-[10px] text-primary">3</span>}</button> })}</div>
    <div className="mt-auto flex flex-col gap-1 border-t border-border pt-4"><button onClick={() => setView('Settings')} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm ${view === 'Settings' ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:bg-muted'}`}><Settings2 className="size-4" />Settings</button><div className="mt-5 flex items-center gap-3 rounded-lg bg-muted/60 p-3"><div className="flex size-8 items-center justify-center rounded-full bg-primary text-xs font-semibold text-primary-foreground">SC</div><div className="min-w-0"><p className="truncate text-xs font-medium">Sam Carter</p><p className="truncate font-mono text-[10px] text-muted-foreground">admin@basestack.io</p></div><MoreHorizontal className="ml-auto size-4 text-muted-foreground" /></div></div>
  </aside>
}

function Topbar({ view, setOpen }: { view: View; setOpen: (v: boolean) => void }) { return <header className="flex h-16 items-center justify-between border-b border-border px-5 lg:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={() => setOpen(true)} aria-label="Open navigation"><Menu className="size-5" /></button><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Broadcast / {view}</p><h1 className="text-lg font-semibold tracking-tight">{view === 'Overview' ? 'Good morning, Sam' : view}</h1></div></div><div className="flex items-center gap-3"><button aria-label="Search" className="hidden rounded-lg border border-border p-2 text-muted-foreground hover:text-foreground sm:block"><Search className="size-4" /></button><button aria-label="Notifications" className="rounded-lg border border-border p-2 text-muted-foreground hover:text-foreground"><Bell className="size-4" /></button><div className="hidden items-center gap-2 border-l border-border pl-3 sm:flex"><StatusDot /><span className="font-mono text-[10px] uppercase text-muted-foreground">All systems operational</span></div></div></header> }

function Metric({ label, value, change, icon: Icon }: { label: string; value: string; change: string; icon: React.ElementType }) { return <div className="rounded-xl border border-border bg-card p-5"><div className="flex items-start justify-between"><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted-foreground">{label}</p><Icon className="size-4 text-primary" /></div><p className="mt-4 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-2 flex items-center gap-1 font-mono text-[10px] text-primary"><ArrowUpRight className="size-3" />{change}</p></div> }

function Overview({ setView }: { setView: (v: View) => void }) { return <div className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="max-w-xl text-sm leading-6 text-muted-foreground">Your communications hub for the Basestack Academy community. Monitor delivery, manage subscribers, and ship better lessons.</p></div><Button onClick={() => setView('Campaigns')} className="gap-2"><Plus className="size-4" />New campaign</Button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Total subscribers" value="2,847" change="12.4% this month" icon={Users} /><Metric label="Delivery rate" value="98.7%" change="0.8% this month" icon={Gauge} /><Metric label="Open rate" value="62.3%" change="4.1% this month" icon={Inbox} /><Metric label="Active campaigns" value="03" change="2 scheduled" icon={Send} /></div><div className="grid gap-6 xl:grid-cols-[1.25fr_0.75fr]"><section className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 className="font-semibold">Recent campaigns</h2><p className="mt-1 text-xs text-muted-foreground">Performance from the last 30 days</p></div><button onClick={() => setView('Campaigns')} className="font-mono text-[10px] uppercase tracking-wide text-primary hover:underline">View all</button></div><div className="flex flex-col">{[['AWS Well-Architected: 5 pillars', 'Sent Aug 24, 2026', '2,810', '64.8%'], ['The cost of being serverless', 'Sent Aug 17, 2026', '2,776', '59.2%'], ['Welcome to Basestack Academy', 'Sent Aug 09, 2026', '2,694', '71.5%']].map((c) => <div key={c[0]} className="flex items-center justify-between gap-4 border-b border-border px-5 py-4 last:border-0"><div className="flex min-w-0 items-center gap-3"><div className="flex size-8 shrink-0 items-center justify-center rounded-lg bg-primary/10"><Mail className="size-4 text-primary" /></div><div className="min-w-0"><p className="truncate text-sm font-medium">{c[0]}</p><p className="font-mono text-[10px] text-muted-foreground">{c[1]}</p></div></div><div className="hidden items-center gap-5 text-right sm:flex"><div><p className="font-mono text-xs">{c[2]}</p><p className="font-mono text-[9px] uppercase text-muted-foreground">delivered</p></div><div><p className="font-mono text-xs text-primary">{c[3]}</p><p className="font-mono text-[9px] uppercase text-muted-foreground">opened</p></div></div></div>)}</div></section><section className="rounded-xl border border-border bg-card"><div className="border-b border-border px-5 py-4"><h2 className="font-semibold">System pulse</h2><p className="mt-1 text-xs text-muted-foreground">Live delivery infrastructure</p></div><div className="flex flex-col gap-5 p-5">{[['Email delivery', 'Operational', 'bg-primary'], ['Subscriber sync', 'Operational', 'bg-primary'], ['Scheduled jobs', 'Operational', 'bg-primary'], ['API health', 'Operational', 'bg-primary']].map(row => <div className="flex items-center justify-between" key={row[0]}><span className="flex items-center gap-2 text-sm"><StatusDot color={row[2]} />{row[0]}</span><span className="font-mono text-[10px] uppercase text-muted-foreground">{row[1]}</span></div>)}<div className="mt-2 rounded-lg bg-muted p-3"><p className="font-mono text-[10px] uppercase tracking-wide text-muted-foreground">Next scheduled send</p><p className="mt-2 flex items-center gap-2 text-sm font-medium"><Clock3 className="size-4 text-primary" />Tomorrow, 09:00 UTC</p></div></div></section></div></div> }

function Campaigns() { const [modal, setModal] = useState(false); const [sent, setSent] = useState(false); return <div className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Create, schedule, and analyze your academy broadcasts.</p></div><Button onClick={() => setModal(true)} className="gap-2"><Plus className="size-4" />New campaign</Button></div><div className="grid gap-4 md:grid-cols-3"><div className="rounded-xl border border-primary/30 bg-primary/5 p-5"><p className="font-mono text-[10px] uppercase tracking-wide text-primary">In progress</p><p className="mt-3 text-3xl font-semibold">01</p><p className="mt-1 text-xs text-muted-foreground">Sending now</p></div><div className="rounded-xl border border-border bg-card p-5"><p className="font-mono text-[10px] uppercase tracking-wide text-muted-foreground">Scheduled</p><p className="mt-3 text-3xl font-semibold">02</p><p className="mt-1 text-xs text-muted-foreground">Ready to send</p></div><div className="rounded-xl border border-border bg-card p-5"><p className="font-mono text-[10px] uppercase tracking-wide text-muted-foreground">Sent this month</p><p className="mt-3 text-3xl font-semibold">08</p><p className="mt-1 text-xs text-muted-foreground">Across 22,418 recipients</p></div></div><div className="rounded-xl border border-border bg-card"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-5"><h2 className="font-semibold">Campaign queue</h2><div className="flex gap-2"><Button variant="outline" size="sm" className="gap-2"><Filter className="size-3" />Filter</Button><Button variant="outline" size="sm" className="gap-2"><Archive className="size-3" />Archive</Button></div></div><div className="flex flex-col">{[['AWS Well-Architected: 5 pillars','Sending now','2,847','64.8%','success'],['The observability starter kit','Scheduled for Aug 29','2,847','—','default'],['Lambda cold starts explained','Draft','—','—','default'],['The cost of being serverless','Sent Aug 17','2,776','59.2%','success']].map(c => <div className="flex flex-col gap-3 border-b border-border p-5 last:border-0 sm:flex-row sm:items-center sm:justify-between" key={c[0]}><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-lg bg-muted"><Mail className="size-4 text-muted-foreground" /></div><div><p className="text-sm font-medium">{c[0]}</p><p className="font-mono text-[10px] text-muted-foreground">{c[1]}</p></div></div><div className="flex items-center gap-6 pl-12 sm:pl-0"><div><p className="font-mono text-xs">{c[2]}</p><p className="font-mono text-[9px] uppercase text-muted-foreground">recipients</p></div><div><p className="font-mono text-xs text-primary">{c[3]}</p><p className="font-mono text-[9px] uppercase text-muted-foreground">open rate</p></div><Badge tone={c[4] as 'success' | 'default'}>{c[1] === 'Draft' ? 'draft' : c[1] === 'Sending now' ? 'live' : 'ready'}</Badge></div></div>)}</div></div>{modal && <div className="fixed inset-0 z-30 flex items-center justify-center bg-foreground/20 p-4"><div className="w-full max-w-lg rounded-xl border border-border bg-card p-6 shadow-xl"><div className="flex items-start justify-between"><div><h2 className="text-lg font-semibold">Create campaign</h2><p className="mt-1 text-sm text-muted-foreground">Start with a simple broadcast brief.</p></div><button onClick={() => setModal(false)} aria-label="Close dialog"><X className="size-5 text-muted-foreground" /></button></div><div className="mt-6 flex flex-col gap-4"><label className="flex flex-col gap-2 text-sm font-medium">Campaign name<input className="rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-primary focus:ring-2" placeholder="e.g. Incident response basics" /></label><label className="flex flex-col gap-2 text-sm font-medium">Subject line<input className="rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-primary focus:ring-2" placeholder="Write a clear, useful subject" /></label><label className="flex flex-col gap-2 text-sm font-medium">Audience<select className="rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-primary focus:ring-2"><option>All subscribers — 2,847</option><option>AWS Student — 1,921</option><option>Newsletter — 926</option></select></label><Button onClick={() => { setSent(true); setModal(false) }} className="mt-2 gap-2">{sent ? <Check className="size-4" /> : <Sparkles className="size-4" />}{sent ? 'Campaign created' : 'Create draft'}</Button></div></div></div>}</div> }

function Subscribers() { const [query, setQuery] = useState(''); const shown = subscribers.filter(s => s.join(' ').toLowerCase().includes(query.toLowerCase())); return <div className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><p className="text-sm text-muted-foreground">Manage consent, audiences, and subscriber health.</p><Button className="gap-2"><Plus className="size-4" />Add subscriber</Button></div><div className="grid gap-4 sm:grid-cols-3"><Metric label="Total" value="2,847" change="12.4% growth" icon={Users} /><Metric label="Subscribed" value="2,801" change="98.4% of total" icon={ShieldCheck} /><Metric label="Pending" value="46" change="Needs confirmation" icon={Clock3} /></div><div className="rounded-xl border border-border bg-card"><div className="flex flex-col gap-3 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><h2 className="font-semibold">All subscribers</h2><div className="relative"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><input value={query} onChange={e => setQuery(e.target.value)} className="w-full rounded-lg border border-input bg-background py-2 pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-primary sm:w-64" placeholder="Search subscribers" /></div></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left"><thead className="bg-muted/50 font-mono text-[10px] uppercase tracking-wide text-muted-foreground"><tr><th className="px-5 py-3 font-normal">Subscriber</th><th className="px-5 py-3 font-normal">Audience</th><th className="px-5 py-3 font-normal">Status</th><th className="px-5 py-3 font-normal">Joined</th><th className="px-5 py-3 font-normal"></th></tr></thead><tbody>{shown.map(s => <tr key={s[1]} className="border-t border-border text-sm"><td className="px-5 py-4"><p className="font-medium">{s[0]}</p><p className="font-mono text-[10px] text-muted-foreground">{s[1]}</p></td><td className="px-5 py-4 text-muted-foreground">{s[2]}</td><td className="px-5 py-4"><Badge tone={s[3] === 'Subscribed' ? 'success' : 'warning'}>{s[3]}</Badge></td><td className="px-5 py-4 font-mono text-xs text-muted-foreground">{s[4]}</td><td className="px-5 py-4"><MoreHorizontal className="size-4 text-muted-foreground" /></td></tr>)}</tbody></table></div></div></div> }

function SimplePage({ view }: { view: View }) { return <div className="rounded-xl border border-border bg-card p-8"><div className="flex size-12 items-center justify-center rounded-xl bg-primary/10"><Code2 className="size-6 text-primary" /></div><h2 className="mt-5 text-xl font-semibold">{view} workspace</h2><p className="mt-2 max-w-md text-sm leading-6 text-muted-foreground">This section is wired into the Broadcast navigation and ready for your content model. Keep your operational surface focused and easy to scan.</p><Button className="mt-6">Configure {view.toLowerCase()}</Button></div> }

export default function BroadcastConsole() { const [view, setView] = useState<View>('Overview'); const [open, setOpen] = useState(false); return <div className="min-h-screen bg-background text-foreground"><div className="flex min-h-screen"><Sidebar view={view} setView={setView} open={open} setOpen={setOpen} /><div className="flex min-w-0 flex-1 flex-col"><Topbar view={view} setOpen={setOpen} /><main className="flex-1 p-5 lg:p-8"><div className="mx-auto max-w-7xl">{view === 'Overview' && <Overview setView={setView} />}{view === 'Campaigns' && <Campaigns />}{view === 'Subscribers' && <Subscribers />}{(view === 'Templates' || view === 'Settings') && <SimplePage view={view} />}</div></main></div></div></div> }

export function PublicSubscribe({ mode = 'subscribe' }: { mode?: 'subscribe' | 'confirmed' | 'unsubscribe' }) { const [submitted, setSubmitted] = useState(mode !== 'subscribe'); if (mode === 'confirmed') return <PublicFrame><Check className="size-6 text-primary" /><h1 className="mt-5 text-3xl font-semibold tracking-tight">You&apos;re confirmed.</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">You&apos;re now on the Basestack Academy broadcast list. Watch your inbox for practical cloud engineering lessons.</p></PublicFrame>; if (mode === 'unsubscribe') return <PublicFrame><X className="size-6 text-primary" /><h1 className="mt-5 text-3xl font-semibold tracking-tight">Unsubscribe from Broadcast</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">We&apos;ll miss you. Confirm below to stop receiving Basestack Academy broadcasts.</p><Button className="mt-7 w-full" onClick={() => setSubmitted(true)}>{submitted ? 'You are unsubscribed' : 'Unsubscribe'}</Button></PublicFrame>; return <PublicFrame><div className="flex size-12 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Activity className="size-6" /></div><p className="mt-7 font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Basestack Academy</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Practical cloud engineering, delivered.</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">A short, useful broadcast for builders working with AWS, infrastructure, and systems that need to stay up.</p><form className="mt-7 flex flex-col gap-3" onSubmit={e => { e.preventDefault(); setSubmitted(true) }}><input required type="email" className="rounded-lg border border-input bg-background px-3 py-3 text-sm outline-none focus:ring-2 focus:ring-primary" placeholder="you@example.com" aria-label="Email address" /><Button type="submit" className="w-full">{submitted ? 'Check your inbox' : 'Subscribe to Broadcast'}</Button></form><p className="mt-5 text-center font-mono text-[10px] text-muted-foreground">No noise. Unsubscribe anytime.</p></PublicFrame> }
function PublicFrame({ children }: { children: React.ReactNode }) { return <main className="flex min-h-screen items-center justify-center bg-muted/40 p-5"><div className="w-full max-w-md rounded-2xl border border-border bg-card p-7 shadow-sm sm:p-10">{children}</div></main> }
